/**
 * SSR вимкнено, передрендер увімкнено.
 *
 * Це застосунок, а не сторінка: усе, що він робить, потребує браузера —
 * дескриптори теки, сокет до бази, `<audio>`, сховище. Серверного рендеру тут
 * нема чого віддавати, зате він додав би цілий клас пасток, у яких код
 * виконується там, де немає `window`.
 *
 * Передрендер лишається: адаптер мусить покласти у `build/` оболонку, з якої
 * стартує клієнт. Наслідок, про який треба пам'ятати: те, що ставить
 * `svelte:head`, у цю оболонку НЕ потрапляє — метатеги живуть у `app.html`.
 */
export const ssr = false;
export const prerender = true;
