/**
 * ФОРМА ДАНИХ ДОШКИ — і хто ЩО пише.
 *
 * Головне правило, з якого виросла вся схема: **у кожного вузла рівно один
 * письменник**. Саме воно дозволяє звузити правила доступу до автора, а не до
 * «будь-кого авторизованого». Зворотний приклад із сусіднього проєкту: партія
 * лежала в одному документі, який писали обидва гравці, — власника не існувало,
 * і єдиним правилом, що не ламало гру, було «дозволити все».
 *
 * | Вузол | Хто пише | Навіщо |
 * |---|---|---|
 * | `info` | господар | назва дошки, хто господар |
 * | `library` | господар (плеєр) | що є в теці |
 * | `hidden/{trackId}` | господар | що не показувати пульту |
 * | `state` | господар (плеєр) | що зараз грає |
 * | `cmd/{id}` | будь-хто на дошці, ЛИШЕ створити | «запусти», «пауза», «гучність» |
 * | `ack/{id}` | господар | «прийняв» або «не вийшло» |
 * | `presence/{uid}` | сам про себе | хто зараз на зв'язку |
 *
 * Поруч, ОКРЕМОЮ ГІЛКОЮ, живе адмінський канал `admin/{ключ}` — те саме
 * влаштування для третьої ролі:
 *
 * | Вузол | Хто пише | Навіщо |
 * |---|---|---|
 * | `info` | господар | хто господар; сам факт існування = пароль правильний |
 * | `tracks` | господар | повні налаштування, яких немає в `library` |
 * | `cmd/{id}` | хто знає адресу, ЛИШЕ створити | «ось нові налаштування», «перечитай папку» |
 * | `ack/{id}` | господар | «прийняв» або «не вийшло» |
 *
 * Чому він не всередині дошки — у `deriveAdminKey`: дозвіл читати поширюється
 * на все піддерево дошки, тож покладений туди канал прочитав би кожен, хто знає
 * пароль дошки, і другий пароль не означав би нічого.
 *
 * ГОСПОДАР — ЦЕ КОМП'ЮТЕР, А НЕ ЛЮДИНА. `ownerUid` — анонімний `uid` того
 * браузера, який дошку створив. Дескриптори теки живуть у тому ж браузері, тож
 * «господар» і «машина, що має доступ до файлів» — це одне й те саме. Друга
 * машина з тією самою парою (ідентифікатор, пароль) зайде на дошку пультом, але
 * плеєром стати не зможе: своєї теки в неї однаково немає.
 */

/** Версія форми даних. Несумісна зміна = нове число й окрема гілка коду. */
export const BOARD_SCHEMA = 1;

export interface BoardInfo {
	/** Назва, яку дала людина. Може бути порожня. */
	name: string;
	/** Анонімний `uid` браузера-господаря. Єдиний, хто пише бібліотеку й стан. */
	ownerUid: string;
	createdAt: number;
	/**
	 * Коли дошку востаннє ВІДКРИВАВ плеєр. Серверний час.
	 *
	 * Окремо від `createdAt`, і це головне поле для прибирання: дошка, яку
	 * відкривають щотижня третій рік, за `createdAt` виглядала б найстарішою з
	 * усіх. Пише його лише господар — пульт присутності дошки не подовжує, бо
	 * інакше забутий планшет у залі тримав би її вічно.
	 *
	 * Поля може не бути: дошки, створені до появи цього коду, його не мають.
	 * Тоді вік рахується від `createdAt` — див. `boardLifetime.ts`.
	 */
	seenAt?: number;
	schema: number;
}

/** Один трек. `id` — ключ у мапі, тож у значенні його немає. */
export interface Track {
	/** Що показати людині. Типово — ім'я файлу без розширення. */
	title: string;
	/**
	 * КЛАВІША, ЯКА ДІЄ, — порахована на ПРИЙМАЧІ.
	 *
	 * Не `hotkey` (то призначений код), а підпис, що справді спрацює, включно з
	 * цифрами за порядком. Рахувати його на пульті самостійно більше не можна:
	 * трек «лише приймач» у списку приймача є й займає свою цифру, а на пульт
	 * не їде, — і власний рахунок пульта розійшовся б із тим, що на екрані в
	 * залі.
	 */
	key?: string;
	/** Шлях усередині обраної теки: `підтека/файл.mp3`. Ключ до дескриптора. */
	path: string;
	/** Тривалість у мілісекундах. `0` — ще не виміряна. */
	durationMs: number;
	/**
	 * ПОРЯДОК У СПИСКУ — і без нього пульт показував би інший.
	 *
	 * `tracks` у RTDB — це МАПА, а мапа порядку не має: діти приходять
	 * упорядкованими за ключем, а ключ тут — хеш шляху. Тобто доти пульт
	 * малював треки в порядку, який не означав нічого, і «двійка» на двох
	 * екранах вказувала на різні треки.
	 */
	order: number;
	/**
	 * Гаряча клавіша — КОД фізичної клавіші (`KeyQ`, `F5`). Не символ:
	 * символ залежить від розкладки, а код — ні.
	 */
	hotkey?: string;
	/** Назва заготовки кольору з `config/trackColors.ts`. */
	color?: string;
	/** Значок перед назвою. Відсутній — без значка. */
	icon?: string;
	/**
	 * Трек запускається за зовнішнім API.
	 *
	 * Сам тригер сюди НЕ їде й не поїде: у ньому адреса чужого сервера й
	 * заголовки із ключем доступу, а бібліотеку читає кожен пульт. Їде лише
	 * відповідь на питання «чому воно заграло саме́» — так, або нічого.
	 */
	auto?: boolean;
}

export interface Library {
	/**
	 * Номер перечитування теки. Пульт порівнює його зі своїм і перемальовує
	 * список лише коли він змінився: інакше кожне оновлення `state` тягло б
	 * перемальовування сотень рядків.
	 */
	rev: number;
	tracks: Record<string, Track>;
}

/** Що робить плеєр ЗАРАЗ. Пише лише він. */
export interface PlayerState {
	/** Що грає. `null` — нічого. */
	trackId: string | null;
	playing: boolean;
	/**
	 * Позиція у треку на момент `atServer`, у мілісекундах.
	 *
	 * Пара «позиція + мить» замість самої позиції — бо інакше пульт мусив би
	 * отримувати оновлення щосекунди, щоб показувати рухому смужку. Із цією
	 * парою він рахує поточну позицію сам, а запис приходить лише на зміну
	 * стану.
	 */
	positionMs: number;
	/**
	 * Тривалість ПОТОЧНОГО треку, мс.
	 *
	 * Живе в стані, а не в бібліотеці, і причина проста: щоб покласти
	 * тривалість у бібліотеку, приймач мусив би розкодувати кожен файл у
	 * теці — сотні читань з диска заради числа, яке потрібне для одного
	 * треку за раз. Рушій знає її тоді, коли трек уже відкрито.
	 */
	durationMs?: number;
	/** Серверний час, на який виміряна `positionMs`. */
	atServer: number;
	/** Гучність 0..1. */
	volume: number;
	/** Чи натиснули на приймачі «Увімкнути звук». Без цього він не грає нічого. */
	armed: boolean;
}

export type CommandType =
	'play' | 'pause' | 'resume' | 'stop' | 'prev' | 'next' | 'volume' | 'seek';

/**
 * ЩО ВМІЄ АДМІНІСТРАТОР — окремий канал, окремий набір.
 *
 * `tracks` несе весь перелік налаштувань одним рядком JSON; `rescan` просить
 * перечитати папку. Обрати саму папку звідси не можна й ніколи не буде можна:
 * браузер видає доступ до теки лише під натисканням людини, яка сидить за цим
 * комп'ютером, — і це та межа, об яку впирається будь-яке віддалене керування.
 */
export type AdminCommandType = 'tracks' | 'rescan';

export interface Command<T extends string = CommandType> {
	/** `uid` того, хто натиснув. Правило звіряє його з автором запису. */
	by: string;
	type: T;
	/** Для `play` — `trackId`; для `volume` — 0..100; для `seek` — позиція в мс. */
	value?: string | number;
	/** Серверний час. Від нього рахується TTL. */
	at: number;
}

/**
 * ПОВНІ НАЛАШТУВАННЯ ТРЕКІВ ДЛЯ АДМІНІСТРАТОРА.
 *
 * Чому не в `library`: там їх бачив би кожен пульт, а серед них є заголовки
 * запиту до чужого API — тобто чужий ключ доступу. Тут їх читає лише той, хто
 * знає адресу каналу.
 *
 * Чому одним рядком: форма налаштувань (тригер із тижневим розкладом
 * усередині) у правилах бази розписувалася б удвічі довше за самі правила й
 * розходилася б із кодом мовчки. Той, хто сюди пише, уже довів знання пароля
 * адресою, а написане однаково перевіряє плеєр, перш ніж покласти у файл.
 *
 * `rev` — той самий лічильник, що й у бібліотеці, і потрібен він рівно для
 * одного: адміністратор надсилає правку разом із номером, який бачив, а плеєр
 * відкидає її, якщо номер уже інший. Інакше двоє, що правлять дошку водночас,
 * мовчки затирали б один одного.
 */
export interface AdminTracks {
	rev: number;
	json: string;
}

export interface Ack {
	ok: boolean;
	/** Ключ перекладу помилки, а не готовий текст: мову обирає той, хто читає. */
	error?: string;
	at: number;
}

/**
 * СКІЛЬКИ ЖИВЕ КОМАНДА.
 *
 * Без межі приймач, який ожив через годину, отримав би від `onChildAdded` увесь
 * накопичений журнал і програв би його підряд — уночі, у порожньому залі. Межа
 * робить команду тим, чим вона є: натисканням, яке або доїхало зараз, або не
 * доїхало взагалі.
 *
 * Шістдесят секунд — це «встиг, поки людина ще дивиться на телефон». Довше не
 * потрібно: людина, чия команда не спрацювала за хвилину, натисне ще раз.
 */
export const COMMAND_TTL_MS = 60_000;

/** Скільки чекати на квитанцію, перш ніж сказати «комп'ютер не відповів». */
export const ACK_TIMEOUT_MS = 6_000;
