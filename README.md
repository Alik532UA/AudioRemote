# AudioRemote

**Один пристрій грає — інші ним керують.**

Аудіодошка для творчої школи. Файли лишаються на тому пристрої, що грає, — на
тому, що стоїть біля колонок. Керувати можна з будь-якого іншого пристрою, і
одразу з кількох: пара «комп'ютер і телефон» тут окремий випадок, а не правило.

Або взагалі без людини: трек можна прив'язати до зовнішнього API — наприклад до
повітряної тривоги.

**Адреса:** https://alik532ua.github.io/AudioRemote/

---

## Як цим користуються

**На пристрої, який гратиме:**

1. Відкрити застосунок → **Створити дошку**.
2. Дати назву («Зал 2»), отримати ідентифікатор і пароль — на кшталт
   `8S7SC` / `МУШЛЯ-МОЛОКО-ОРБІТА-4723`.
3. Натиснути **«Увімкнути звук»**, якщо браузер попросить. У застосунку для
   стаціонарного комп'ютера цього кроку немає.
4. Обрати папку з музикою. Застосунок вичитає з неї всі аудіофайли; зайві можна
   приховати від пульта.
5. **Лишити вікно відкритим** — саме воно грає.

У браузері папку доводиться обирати після кожного перезавантаження сторінки, і
це не недоробка інтерфейсу — причина названа в `PROJECT-CONTEXT.md` § 5.0.
Застосунок для стаціонарного комп'ютера (`src-tauri/`) цієї межі не має: там
папка — звичайний шлях, і він пам'ятається.

**З пульта — будь-який інший пристрій:**

1. Відкрити застосунок → **Підключитися**.
2. Ввести ідентифікатор і пароль, які продиктували.
3. Натискати треки.

Регістр, пробіли замість дефісів і незмінена розкладка на 13 схожих літерах
значення не мають — усе це нормалізується.

**Налаштувати дошку, не встаючи з місця (адміністратор):**

Щоб змінити кількість повторів чи прибрати трек, не обовʼязково йти до
пристрою в залі. На плеєрі один раз вмикається другий пароль — **«Підключити
пульт» → «Дозволити налаштування з пульта»**. Пульт, який його введе
(**«Адміністратор»** у шапці дошки), дістає те саме вікно налаштувань треку, що
й людина за самим плеєром: підписи, кольори, клавіші, повтори, видимість, запуск
за API, порядок.

Єдине, чого звідти не зробити, — обрати папку: браузер видає доступ до неї лише
під натисканням людини за тим самим пристроєм.

## Чого цей застосунок НЕ робить

- **Не завантажує файли нікуди.** Ні в хмару, ні на сервер. Сторінка отримує
  дескриптор папки від людини, і цей дескриптор не покидає пристрою.
- **Не працює із закритим вікном.** Вебсторінка не може розбудити браузер.
- **Не дає відкликати доступ окремій людині.** Пароль — це адреса дошки; хто
  його знає, той усередині. Щоб закрити доступ, треба створити дошку заново.
  Адміністратора при цьому вимкнути МОЖНА окремо: у нього свій пароль і свій
  канал, і вимкнення не чіпає доступ до самої дошки.

## Запуск

Потрібні Node 22.12+ і Java 21 (її вимагає емулятор Firebase).

```bash
npm install
```

Далі **два термінали**. Перший — база:

```bash
npm run emulators
```

Другий — застосунок:

```bash
npm run dev
```

Без емулятора сторінка скаже про це прямо: анонімний вхід нікуди не доїде.

## Перевірки

```bash
npm run check         # типи (обов'язково перед комітом правок .svelte)
npm run lint          # ідіоми
npm test              # юніт-перевірки; серед них — перевірки над самим проєктом:
                      # закінчення рядків, стан workflow, базовий набір eslint,
                      # досяжність модулів, розмір файлів, імена керування,
                      # факти в цих документах
npm run check:runners # чи всі перевірки хтось запускає (окремим процесом — навмисно)
npm run check:rules   # правила бази на емуляторі: 20 дозволів і 41 заборона
npm run test:e2e      # браузер над зібраною build/ (спершу npm run build)
```

Збірка й гейти над нею:

```bash
MSYS_NO_PATHCONV=1 BASE_PATH=/AudioRemote npm run build
npm run check:build   # маніфест, воркер, CSP і хеші інлайн-скриптів
npm run check:bundle  # критичний шлях сторінки: скрипти й стилі, КБ gzip
```

## Застосунок для стаціонарного комп'ютера

`src-tauri/` — тонка оболонка навколо ТІЄЇ САМОЇ сторінки, що лежить за адресою
вище. Вона не вшита в exe, а вантажиться з мережі, тож зміна коду доїжджає
звичайним деплоєм, і перевстановлювати нічого не треба. У exe живе рівно те,
чого браузер не вміє: читання папки з музикою за шляхом, тобто папка за
замовчуванням.

Потрібні Rust (MSVC) і C++ Build Tools:

```bash
winget install Rustlang.Rustup
winget install Microsoft.VisualStudio.2022.BuildTools --override "--quiet --wait --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended"
```

```bash
npm run tauri:dev     # вікно з бойовою сторінкою
npm run tauri:build   # інсталятор NSIS у src-tauri/target/release/bundle
```

## Як воно влаштоване

| Шар          | Де                                             | Про що                                                   |
| ------------ | ---------------------------------------------- | -------------------------------------------------------- |
| Адреса дошки | [`src/lib/board/`](src/lib/board/)             | пароль входить у шлях; уся безпека тут                   |
| Мережа       | [`src/lib/net/`](src/lib/net/)                 | RTDB: опис, бібліотека, стан, журнал команд, присутність |
| Адмінканал   | [`src/lib/net/admin.ts`](src/lib/net/admin.ts) | другий пароль — друга адреса; правки з пульта            |
| Звук         | [`src/lib/audio/`](src/lib/audio/)             | папка з музикою, озброєння, відтворення                  |
| Плеєр        | [`src/lib/player/`](src/lib/player/)           | виконує команди, оголошує стан                           |
| Пульт        | [`src/lib/remote/`](src/lib/remote/)           | надсилає команди, показує стан                           |

Правила доступу — [`database.rules.json`](database.rules.json). Це **єдина межа
безпеки**: публічний конфіг Firebase лежить у бандлі, тож усе, що робить наш
код, може зробити будь-хто з консолі браузера.

Параметри проєкту, свідомі відхилення від канону й **названі межі** —
[`PROJECT-CONTEXT.md`](PROJECT-CONTEXT.md). Прочитати варто до першої правки:
там записано, зокрема, чому плеєр у браузері працює лише в Chrome/Edge на
стаціонарному комп'ютері й чому зміна пароля означає нову дошку.
